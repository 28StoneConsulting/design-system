import { Meta, Typeset } from '@storybook/addon-docs/blocks';
import ThemeProvider from './theme-provider.tsx';

<Meta title="Typography" parameters={{ chromatic: { disable: true } }} />

# Typography

#

## Nunito Sans

**Weight:** 400(Regular)

<ThemeProvider>
  {({ theme }) => (
    <Typeset
      fontSizes={[
        Number(theme.typography.size.s1),
        Number(theme.typography.size.s2),
        Number(theme.typography.size.s3),
        Number(theme.typography.size.m1),
        Number(theme.typography.size.m2),
        Number(theme.typography.size.m3),
        Number(theme.typography.size.l1),
        Number(theme.typography.size.l2),
        Number(theme.typography.size.l3),
      ]}
      fontWeight="400"
    />
  )}
</ThemeProvider>

**Weight:** 600(Semi Bold)

<ThemeProvider>
  {({ theme }) => (
    <Typeset
      fontSizes={[
        Number(theme.typography.size.s1),
        Number(theme.typography.size.s2),
        Number(theme.typography.size.s3),
        Number(theme.typography.size.m1),
        Number(theme.typography.size.m2),
        Number(theme.typography.size.m3),
        Number(theme.typography.size.l1),
        Number(theme.typography.size.l2),
        Number(theme.typography.size.l3),
      ]}
      fontWeight="600"
    />
  )}
</ThemeProvider>

**Weight:** 700(Bold)

<ThemeProvider>
  {({ theme }) => (
    <Typeset
      fontSizes={[
        Number(theme.typography.size.s1),
        Number(theme.typography.size.s2),
        Number(theme.typography.size.s3),
        Number(theme.typography.size.m1),
        Number(theme.typography.size.m2),
        Number(theme.typography.size.m3),
        Number(theme.typography.size.l1),
        Number(theme.typography.size.l2),
        Number(theme.typography.size.l3),
      ]}
      fontWeight="700"
    />
  )}
</ThemeProvider>

**Weight:** 800(Extra Bold)

<ThemeProvider>
  {({ theme }) => (
    <Typeset
      fontSizes={[
        Number(theme.typography.size.s1),
        Number(theme.typography.size.s2),
        Number(theme.typography.size.s3),
        Number(theme.typography.size.m1),
        Number(theme.typography.size.m2),
        Number(theme.typography.size.m3),
        Number(theme.typography.size.l1),
        Number(theme.typography.size.l2),
        Number(theme.typography.size.l3),
      ]}
      fontWeight="800"
    />
  )}
</ThemeProvider>

**Weight:** 900(Black)

<ThemeProvider>
  {({ theme }) => (
    <Typeset
      fontSizes={[
        Number(theme.typography.size.s1),
        Number(theme.typography.size.s2),
        Number(theme.typography.size.s3),
        Number(theme.typography.size.m1),
        Number(theme.typography.size.m2),
        Number(theme.typography.size.m3),
        Number(theme.typography.size.l1),
        Number(theme.typography.size.l2),
        Number(theme.typography.size.l3),
      ]}
      fontWeight="900"
    />
  )}
</ThemeProvider>

## Calibri

**Weight:** 400(Regular)

<ThemeProvider>
  {({ theme }) => (
    <Typeset
      fontFamily="Calibri"
      fontSizes={[
        Number(theme.typography.size.s1),
        Number(theme.typography.size.s2),
        Number(theme.typography.size.s3),
        Number(theme.typography.size.m1),
        Number(theme.typography.size.m2),
        Number(theme.typography.size.m3),
        Number(theme.typography.size.l1),
        Number(theme.typography.size.l2),
        Number(theme.typography.size.l3),
      ]}
      fontWeight="400"
    />
  )}
</ThemeProvider>

**Weight:** 600(Semi Bold)

<ThemeProvider>
  {({ theme }) => (
    <Typeset
      fontFamily="Calibri"
      fontSizes={[
        Number(theme.typography.size.s1),
        Number(theme.typography.size.s2),
        Number(theme.typography.size.s3),
        Number(theme.typography.size.m1),
        Number(theme.typography.size.m2),
        Number(theme.typography.size.m3),
        Number(theme.typography.size.l1),
        Number(theme.typography.size.l2),
        Number(theme.typography.size.l3),
      ]}
      fontWeight="600"
    />
  )}
</ThemeProvider>

**Weight:** 700(Bold)

<ThemeProvider>
  {({ theme }) => (
    <Typeset
      fontFamily="Calibri"
      fontSizes={[
        Number(theme.typography.size.s1),
        Number(theme.typography.size.s2),
        Number(theme.typography.size.s3),
        Number(theme.typography.size.m1),
        Number(theme.typography.size.m2),
        Number(theme.typography.size.m3),
        Number(theme.typography.size.l1),
        Number(theme.typography.size.l2),
        Number(theme.typography.size.l3),
      ]}
      fontWeight="700"
    />
  )}
</ThemeProvider>

**Weight:** 800(Extra Bold)

<ThemeProvider>
  {({ theme }) => (
    <Typeset
      fontFamily="Calibri"
      fontSizes={[
        Number(theme.typography.size.s1),
        Number(theme.typography.size.s2),
        Number(theme.typography.size.s3),
        Number(theme.typography.size.m1),
        Number(theme.typography.size.m2),
        Number(theme.typography.size.m3),
        Number(theme.typography.size.l1),
        Number(theme.typography.size.l2),
        Number(theme.typography.size.l3),
      ]}
      fontWeight="800"
    />
  )}
</ThemeProvider>

**Weight:** 900(Black)

<ThemeProvider>
  {({ theme }) => (
    <Typeset
      fontFamily="Calibri"
      fontSizes={[
        Number(theme.typography.size.s1),
        Number(theme.typography.size.s2),
        Number(theme.typography.size.s3),
        Number(theme.typography.size.m1),
        Number(theme.typography.size.m2),
        Number(theme.typography.size.m3),
        Number(theme.typography.size.l1),
        Number(theme.typography.size.l2),
        Number(theme.typography.size.l3),
      ]}
      fontWeight="900"
    />
  )}
</ThemeProvider>
